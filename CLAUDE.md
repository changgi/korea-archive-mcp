# korea-archive-mcp 개발 원칙

Vercel 원격 MCP 서버(Node, [route.js](app/api/[transport]/route.js)) — 웹·모바일 사용자가
커넥터로 접속한다. 자매 저장소 `D:\korea-archive-marketplace`(Python 플러그인 서버)와 항상 패리티 유지.

## 제1원칙 — 웹·모바일 동등성: "지식은 서버로, 실행은 환경별 경로로"

Claude Code 스킬(`.claude/skills/`)은 **로컬 전용**이다. 웹·모바일 사용자는 MCP 도구 응답만 받는다.
따라서 새 기능·노하우를 만들 때마다:

1. **지식(규칙·템플릿·디자인 시스템·프롬프트)은 반드시 MCP 도구로도 노출한다** — 스킬에만 넣으면 웹에서는 존재하지 않는 기능이다.
   (예: `report_template`의 kind 모드 — report / carousel / canva_prompts)
2. **로컬 실행 의존 단계(헤드리스 크롬·Python 스크립트·파일 생성)는 웹 대체 경로를 도구 응답 규칙에 명시한다**
   (예: 캐러셀 렌더 — 로컬=렌더 스크립트, 웹=아티팩트 미리보기 또는 Canva 커넥터 생성→PNG 내보내기).
3. 스킬을 수정하면 같은 내용의 서버 상수도 갱신한다(스킬-서버 패리티). 캐노니컬 원본은 스킬 에셋,
   서버 반영은 스플라이스 스크립트로.

## PlayMCP 제약 (위반 시 심사 반려)

- 도구 **20개 이하** — 새 도구 대신 기존 도구의 파라미터 모드(enum)로 확장한다.
- 도구 응답 **24KB 이하** — 텍스트 페이로드 증보 시 UTF-8 **바이트** 실측 필수(한글 3B/자).
- 전 도구 description에 서비스명 **"KOREA ARCHIVE 통합검색"** 포함(래퍼가 자동 부착 — SVC 상수 유지).
- 전 도구 annotations(read-only) — server.tool 래퍼가 일괄 적용.
- 도구 스키마·description 변경 후에는 PlayMCP 콘솔 **"정보 불러오기"** 재실행을 사용자에게 안내한다.

## 배포·검증 절차 (모든 서버 변경 시)

1. route.js 수정 → `node --check`(.mjs 사본으로) 통과 확인.
2. marketplace server.py 패리티 반영 → `py_compile` 통과 확인.
3. 두 저장소 커밋·푸시 — main 푸시 시 Vercel 자동 배포(리전 icn1). 중요 변경은 배포 READY 상태까지 확인.
4. 다국어 변환(TOPICS)·보고서 규칙 등 공유 상수는 양쪽 동일해야 한다.

## 콘텐츠 신뢰 원칙 (도구 응답·산출물 공통)

- 실물 기록만 — AI 생성 인물·역사 장면 금지. Canva 등 생성 AI 사용 시 실물 자산 강제 + 생성 후 한국어 환각 문구 전수 교정.
- 링크·수치·훈격은 도구 호출·API 원문으로 실확인한 것만. 정정 시 정정 이력 명기.
- 협약기관(KOREAN WAR ARCHIVES 6·25전쟁 아카이브센터) 기록은 출처 표기 필수. 게재윤리 4단계(포로·사망자 존엄) 적용.
